# 任务列表

    [ ] 代表完全未开始处理   [-] 代表开始处理中   [*] 代表处理完成但是未经过人工验收   [x] 代表处理完成并且经过人工验收

## UI/UX 优化

- [x] 用量成本页中的费用“仅统计 Token 与价格均已知的请求”这句文案不需要单独占一行，可以用一个小提示 icon 放在模块标识旁边，然后鼠标悬浮展示吧
- [ ] 用量成本页的 Tokens 日历热力图样式整体要优化下，另外鼠标悬浮上去的 token 信息展示应该和其他页面的 token 信息展示一样
- [x] 用量成本页的时间筛选 select 应该放在实时更新中的左边，保持实时刷新控件在最右侧，和其他页面保持一致
- [x] 会话管理页的客户端筛选 select 宽度调整：去掉内联 `width:100%`（原被撑满太宽），并把 CSS `.toolbar :global(.semi-select)` 从 132px 提到 210px（132px 装不下"ChatGPT (Codex)"/"未经过 Flowlet"等较长选项，客户端与 Flowlet 状态两个 Select 统一 210px）
- [-] 概览页 - 渠道账号中的 LongCat 有效期、Qwen 个人版 Token Plan 7天重置时间，如果日期已是当天，展示为时分秒（LongCat 官方过期时间为当天 23:59:59，忽略原始时分秒；Qwen 展示获取到的实际时分秒）
- [ ] Codex 账号与用量里的最后更新时间支持鼠标悬浮上去查看下次刷新数据时间
- [x] 请求日志页、会话管理页、用量成本页里的费用都应该是四位小数，且就算后面都是 0 也要补 0 补到四位

## Agent 接入

- [ ] 如果 Agent 没有安装，支持在 Agent 详情弹窗中添加相关安装文档的指引
- [ ] 支持检查 Agent 最新版本号，并提示用户可以升级，如果可以的话再加上更新版本记录（并支持翻译成目标语言）这个要做成定时任务后台异步执行，执行记录也要进任务日志里

## 请求日志

- [ ] 性能统计上增加 RPM、TPM 的概念，耗时也需要再拆分下 TTFB、TTFT
- [x] 请求明细改为文件存储、SQLite 保留索引与统计字段，并继续支持按时间和体积清理
- [-] Windows 待机恢复后请求失败（其实根因应该是待机中应该也要能运行才对）./docs/windows-suspend-resume-network-resilience.md
- [ ] 设置界面可以新出一个配置项，货币或者说是计价方式，目前支持美元和人民币两种，然后模型也分两种货币方式，一种是官方就同时支持两种货币的，那么取官方价，还有一种可能官方只有美元或者只有人民币的，那么就需要汇率换算，汇率就先 config.json 中配死一个即可
- [-] 请求日志详情弹窗里的概览信息可以加一个会话 id 的露出并且支持复制和点击进行筛选，类似会话管理会话详情弹窗中的功能
- [x] 我修改了账号名称后，请求日志里的渠道账号名称并没有改变

## 模型服务

- [-] 模型的一些基础信息包括定价改成从我们另一个项目公开的数据获取：请按照 docs/agent-integration-prompt.md 的规则，将 models-cn 接入当前项目。数据地址：https://null-object-0000.github.io/models-cn/api.json 目标：读取中国大陆模型厂商的官方价格和模型信息，并实现可测试的模型查询与费用估算能力。要求：人民币官方价格优先；不得硬编码价格或用汇率伪造人民币官方价；官方字段缺失时允许使用 models.dev 补全，但必须保留参考来源，不能覆盖官方值；正确处理币种、市场和标准价/优惠价；仅在 `input.cacheHit` 存在时处理缓存命中价格。请先检查当前项目技术栈和已有模型配置，再实施修改、运行测试，并说明改动文件及使用方式。
- [x] 直接渠道模型的详情我觉得可以引入 Tab 隔离基础信息（模型参数等）、价格信息（这里的价格是指这个模型在原厂商直接 API 售卖的方式下的价格，和不同的渠道账号无关）、渠道路由三个板块
- [x] 新增账号后其对应的相关模型自动进入路由，这一步没问题，但是不应该是默认开启的，应该是关闭的，用户需要再去手动开启（除非是第一个账号，才默认开启）

## 渠道账号

- [x] Qwen 的渠道账号管理详情弹窗里的订阅信息完全去除手动维护的逻辑和代码，另外整体的 UI 改成 LongCat 资源包信息那样，只是把从左到右四个模块的第一个模块剩余额度改成 5 小时的额度情况，第二个模块的消耗进度是7天的额度情况，然后第三个模块最早到期改成整个套餐的到期时间。另外额度消耗进度的展示模块改成上面是剩余xx% 总量xxtoken，然后中间仍然是进度条，底部原先是总量 6500万 Token改成额度重置时间的信息露出
- [ ] 如果用户有添加 chatgpt（codex）账号，那么就把伪装成要给渠道账号，不能编辑、不能打开，只是能在概览页的渠道账号模块被看到，管理账号的列表弹窗也不能看到。展示的元素类似 qwen 的 token plan 模式

## 会话管理

- [-] 会话管理里读取各个 Agent 的状态时最好看能不能读到这个会话是不是在进行中（这里面要改还能细分为，自动运行中，运行中但当前需要用户确认）

## 用量陈本

- [ ] 用量成本里没有把没经过 flowlet 的费用算在里面，当然也可以出一个筛选项或者开关看是近仅看经过 flowlet 的部分还是看全部，亦或者只看未经过的

## 未来规划

- [-] 我想通过内置浏览器内容让用户在我们 Flowlet 的环境里进行官网 Web 登陆然后我们就能自动解析出那些不支持 API 方式查询的套餐余量或是 API 余额了
- [ ] 可以出一个任务分发功能，我们只管提交 todo，并且分配给不同的 AI Agent，然后 Flowlet 来调度（这里一定需要用到工作树，不然太乱了）
- [ ] 要想一种办法可以让多个设备上的数据互相都能看到，但明细数据不需要混合在一起，可以按设备分开筛选查看，用量成本这类统计向的数据需要有汇总的版本，当然也可以查看指定设备上的情况
- [ ] 要重前端轻后端，并且要重点看下后端代码是否有超大文件，前端也可以顺带看下
- [ ] 可以出一个订阅或者说账单管理，基于我们的内置 WebView 能力拉取到各种订阅、充值记录
- [ ] 需要出一个 MacOS 的 Widget，能显示当前的用量成本和请求日志的最新情况，最好还能直接在 Widget 上操作一些简单的功能，比如刷新、切换设备、切换账号等
- [ ] 我应该要能有个地方看到指定渠道账号或者账号下的具体某个模型的性能情况

## 性能优化

- [x] 检查并修复历史数据点击修复按钮后，整个应用很卡顿，还会无响应
- [x] 用量成本页现在点进去数据会先空很长一段时间，看是否可以优化性能还是说用更好的骨架屏、过度效果，亦或者单独出一张统计表起一个定时任务定时清晰？
- [x] LongCat 账号初次点立即刷新很慢才弹出登录框让我登录，并且登录的过程中感觉被什么东西隐藏，会触发自动的页面刷新，不知道是不是定时任务，如果是的话，定时任务应该要知道我当前是未登录状态，应该跳过执行
